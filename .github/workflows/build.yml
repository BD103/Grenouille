name: Build Binaries

on:
  push:
    branches:
      - main
      # TODO: Delete this
      - build-ci

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello, world!"
